import { scaleSize, px2dp } from './ScreenUtils'

export default {
    container:　{
        flex: 1,
        fontSize: px2dp(22),
        paddingLeft: scaleSize(40),
        paddingRight: scaleSize(40),
        width: scaleSize(750)
    }
}